import { createStitches } from '@stitches/react'

export const { 
  config, 
  styled,
  globalCss,
  createTheme,
  css,
  getCssText,
  keyframes,
  theme
} = createStitches({
  theme: {
    colors: {
      rocketseat: '#8257e6'
    }
  }
})